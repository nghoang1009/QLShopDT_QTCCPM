name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  # =====================
  # STAGE 1: LINT
  # =====================
  lint:
    name: Lint & Syntax Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: PHP Syntax Check
        run: |
          find ./QLShopDT_API -name "*.php" -exec php -l {} \;
          echo "✅ PHP syntax check passed"

  # =====================
  # STAGE 2: BUILD & TEST
  # =====================
  build-and-test:
    name: Build Docker & Run Playwright Tests
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v4

      # Build và khởi động Docker containers
      - name: Build and start containers
        run: docker compose up -d --build

      # Chờ MySQL sẵn sàng (tránh import DB quá sớm)
      - name: Wait for MySQL to be ready
        run: |
          echo "Waiting for MySQL..."
          DB_CONTAINER=$(docker compose ps -q db)
          for i in $(seq 1 60); do
            if docker exec $DB_CONTAINER mysqladmin ping -u root -prootpass --silent 2>/dev/null; then
              echo "✅ MySQL is ready"
              break
            fi
            echo "Attempt $i/60..."
            sleep 3
          done

      - name: Import database
        run: |
          DB_CONTAINER=$(docker compose ps -q db)
          docker exec -i $DB_CONTAINER mysql -u root -prootpass qlshopdienthoai < qlshopdienthoai.sql
          echo "✅ Database imported"

      - name: Health check
        run: |
          APP_CONTAINER=$(docker compose ps -q app)
          for i in $(seq 1 10); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/QLShopDT_API/app.php)
            if [ "$STATUS" = "200" ] || [ "$STATUS" = "302" ]; then
              echo "✅ App is up (HTTP $STATUS)"
              break
            fi
            echo "Attempt $i/10 - HTTP $STATUS..."
            sleep 3
          done

      # Cài Node + dependencies
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'npm'

      - name: Install Node dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      # Chạy toàn bộ bộ test Playwright
      - name: Run Playwright tests
        run: npx playwright test --workers=1
        env:
          CI: true
          BASE_URL: http://localhost:8080

      # Upload báo cáo test (luôn upload dù pass hay fail)
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      # Dọn dẹp container sau khi test xong
      - name: Stop containers
        if: always()
        run: docker compose down

  # =====================
  # STAGE 3: DEPLOY
  # =====================
  deploy:
    name: Deploy (Build Production Image)
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      - name: Build production Docker image
        run: |
          docker build -t qlshopdt-api:latest .
          echo "✅ Production image built successfully"

      - name: Show image info
        run: docker images qlshopdt-api